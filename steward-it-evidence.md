# Steward-it evidence

Working evidence from real Agentic Engineering use. Keep entries compact: normally one or two sentences, with a concrete commit, PR, issue, or session reference when available. Record observations, not proposed canonical fixes, until repeated evidence justifies one.

- **2026-09-10:** A `lab-it` fork delegated only to extract design evidence independently ran Plan Synthesis after inheriting the parent's full skill context, temporarily producing an unapproved `plan.md`; the parent caught and reverted it before it became authoritative. Evidence: useOrbit Policies investigation, session skill trace; resulting plan committed at `c258d30`.
- **2026-09-10:** In the same Policies planning session, `lab-it` loaded two files (~5k rough tokens), while `plan-it` loaded six supporting rule files (~15–16k rough tokens); several sections were not materially used for this schema-only pass. These are static/agent-reported estimates, not measured tokenizer usage, so retain as a context-cost baseline rather than a confirmed inefficiency.
- **2026-09-10:** useOrbit issue #313 spent a substantial share of implementation time on full-suite verification; a restore-and-rerun cycle was needed to isolate a pre-existing warning, and `review-it` re-ran the full suite after fixing one schema-length finding. Evidence: issue #313 session; 897/897 passed with `--no-tia`.
- **2026-09-10:** The #313 flow showed a useful testing-boundary question: targeted tests were fast and iterative, while repeated full-suite runs materially increased elapsed time; the current issue-by-issue milestone model may justify reserving full-suite verification as a human-controlled checkpoint rather than requiring it for every ready issue. Evidence: issue #313 session; `--no-tia` full-suite runs and Gate 1 review.
- **2026-09-11:** useOrbit issue #314 under v2.0.4 completed the implement→verify→review→Gate 1→Gate 2→commit portion in ~5m51s from the first scaffold write to commit, but this excludes earlier research, so it is a partial speed datapoint rather than a whole-issue benchmark. Evidence: useOrbit issue #314, commit `de62550`.
- **2026-09-11:** `implement-it`'s new full-suite choice was applied incorrectly in #314: the human was asked to run/skip only after Gate 1, Gate 2, and commit, rather than after targeted verification and before Gate 1 as the skill requires. Evidence: useOrbit issue #314 session; full-suite choice was still recorded and the human chose skip.
- **2026-09-11:** During #314, `implement-it` did not load the standing Laravel/project companion skills (`my-laravel-patterns`, `my-phpstorm-conventions`, Laravel Boost testing/pattern skills), instead mirroring the approved Medical implementation; the result passed review, but the documented skill-composition contract was not followed. Evidence: useOrbit issue #314 session; review-it confirmed the implementation clean.
- **2026-09-11:** The prohibited `Co-Authored-By` trailer recurred during #314 despite existing project memory, making this the third recorded occurrence of the same adherence failure; the issue was caught by the human before finalizing the commit. Evidence: useOrbit issue #314 session and `feedback_commit_workflow.md` history.
- **2026-09-11:** A #314 empirical probe confirmed Laravel's `for()` relationship binding did not recursively create an extra parent when used with the child factory's default parent configuration, so the new factory rule was supported by the broader pattern but did not actually prevent a recursion bug in this implementation. Evidence: useOrbit issue #314 session; transaction-wrapped Tinker probe.
- **2026-09-11:** The #314 session exposed only a coarse ~41k-token session-budget delta, not per-skill attribution; this is useful as a runtime pressure datapoint but not a reliable tokenizer measurement. Evidence: useOrbit issue #314 execution report.
- **2026-09-11:** After #314, repeated `Co-Authored-By` violations were promoted from project-memory evidence to an `implement-it` commit-construction rule, and commit subjects were narrowed to concise implementation-outcome sentences. Evidence: useOrbit #314; `implement-it` `commit-boundaries.md` updates on `steward-it`.
